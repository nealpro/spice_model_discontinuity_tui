io:
  output_dir: "output_directory"
  files: ["tests/files/generated_demo_discontinuous.csv"]

detection:
  sensitivity: 1.0 # standard deviations
  min_prominence: 20.0 # scipy.find_peaks
  min_separation: 3

analysis:
  independent_col: "x" # CSV column to use as the x-axis
