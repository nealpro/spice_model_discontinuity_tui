io:
  output_dir: "output_directory"
  files: ["tests/files/example_input.csv"]

detection:
  sensitivity: 1.0 # standard deviations
  min_prominence: 20.0 # scipy.find-peaks
  min_separation: 3

analysis:
  independent_col: "x" # chosen x_value
