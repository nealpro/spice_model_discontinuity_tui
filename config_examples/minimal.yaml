io:
  output_dir: "output_directory"
  inputs: [ # Inputs array
    "spice_files1/", # Can be a directory
    "other_dir/specific_file.csv", # Can be a file
    "spice_files2/" # Path can be relative to current directory or from /
  ]

detection:
  # Configures median absolute deviation step 
  sensitivity: 1.0 # standard deviations
  # Configures scipy.find_peaks
  min_prominence: 20.0
  min_separation: 3


